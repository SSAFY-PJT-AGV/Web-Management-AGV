spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_factory
    username: root
    password: ssafy

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    jackson:
      serialization:
        fail-on-empty-beans: false

    open-in-view: false